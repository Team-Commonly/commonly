# Commonly — Local Install
# Local, single-machine setup. MongoDB only, no cloud dependencies.
#
# Usage:
#   ./install.sh
#
# `install.sh` creates an ignored .env with a strong JWT secret when needed.
# This profile binds the app to localhost-oriented URLs; do not use it as a
# public-domain/TLS deployment.

services:
  mongo:
    image: mongo:7
    container_name: commonly-mongo
    restart: unless-stopped
    volumes:
      - mongo-data:/data/db
    networks:
      - commonly
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.runCommand({ ping: 1 })"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: commonly-backend
    restart: unless-stopped
    depends_on:
      mongo:
        condition: service_healthy
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000

      # Database — MongoDB only, no PostgreSQL required
      - MONGO_URI=mongodb://mongo:27017/commonly

      # Auth — install.sh creates JWT_SECRET in the ignored root .env file.
      - JWT_SECRET=${JWT_SECRET:?Run ./install.sh or set JWT_SECRET in .env before starting Commonly.}

      # Registration — open by default for local installs
      # Set REGISTRATION_INVITE_ONLY=true and REGISTRATION_INVITE_CODES=code1,code2 to restrict
      - REGISTRATION_INVITE_ONLY=${REGISTRATION_INVITE_ONLY:-false}
      - REGISTRATION_INVITE_CODES=${REGISTRATION_INVITE_CODES:-}

      # Frontend CORS
      - FRONTEND_URL=http://localhost:3000

      # Optional integrations — leave blank to disable
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
      - DISCORD_CLIENT_ID=${DISCORD_CLIENT_ID:-}
      - DISCORD_CLIENT_SECRET=${DISCORD_CLIENT_SECRET:-}
      - DISCORD_BOT_TOKEN=${DISCORD_BOT_TOKEN:-}
      - DISCORD_PUBLIC_KEY=${DISCORD_PUBLIC_KEY:-}
      - SENDGRID_API_KEY=${SENDGRID_API_KEY:-}
      - SENDGRID_FROM_EMAIL=${SENDGRID_FROM_EMAIL:-}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - BRAVE_API_KEY=${BRAVE_API_KEY:-}

      # Agent runtime — set a shared secret for CAP connections
      - CLAWDBOT_GATEWAY_TOKEN=${CLAWDBOT_GATEWAY_TOKEN:-}
      - OPENCLAW_RUNTIME_TOKEN=${OPENCLAW_RUNTIME_TOKEN:-}
    networks:
      - commonly

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - REACT_APP_API_URL=http://localhost:5000
    container_name: commonly-frontend
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "3000:80"
    networks:
      - commonly

networks:
  commonly:
    driver: bridge

volumes:
  mongo-data:
