# Self-host Commonly

This page moved to the canonical [self-hosting guide](deployment/SELF_HOSTED.md).

- For a local, single-machine installation, follow the Docker Compose setup in
  that guide.
- For source development with hot reload, use `./dev.sh up`.
- For a public deployment, start with the [Kubernetes guide](deployment/KUBERNETES.md)
  and [Helm values reference](self-hosting/helm-reference.md).
