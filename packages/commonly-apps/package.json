{
  "name": "@commonly/apps",
  "version": "0.1.0",
  "description": "First-party native agents for Commonly — declarative app definitions that run on the native runtime.",
  "type": "commonjs",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "tsc:check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
